<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.parkdusang.homework2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListActivity" />
        <activity android:name=".Tipcalculator" />
        <activity android:name=".Timetable" />
        <activity android:name=".MiniCalculator" />
        <activity android:name=".InformationTimetTable" />
        <activity android:name=".InformationTipcalculator" />
        <activity android:name=".InformationMiniCalculator" />
        <activity android:name=".ChangePwd"></activity>
    </application>

</manifest>
